@using Vendr.Extensions;
@inherits Umbraco.Web.Mvc.UmbracoViewPage<Vendr.DemoStore.Models.CheckoutReviewPage>
@{
    Layout = "CheckoutStepPage.cshtml";
}

@Html.Partial("OrderInformationSummary")

@using (Html.BeginPaymentForm(Model.Order))
{
    <div class="border border-gray-300 rounded">
        <label class="flex items-center py-4 px-4 cursor-pointer hover:bg-gray-100">
            <input id="acceptTerms" type="checkbox" value="1" class="mr-3" />
            <span class="font-medium">Ik accepteer de algemene voorwaarden</span>
        </label>
    </div>

    <div class="flex flex-col-reverse items-center justify-between mt-8 md:flex-row">
        @if (Model.PreviousStep != null)
        {
            <div><a href="@(Model.PreviousStep.Url)" class="text-@(Model.CheckoutPage.PrimaryColor) hover:text-gray-900 hover:underline"><i class="fa fa-angle-left"></i> Terug naar @(Model.PreviousStep.Name)</a></div>
        }
        else
        {
            <div><a href="@(Model.HomePage.CartPage.Url)" class="text-@(Model.CheckoutPage.PrimaryColor) hover:text-gray-900 hover:underline"><i class="fa fa-angle-left"></i> Terug naar winkelmand</a></div>
        }
        <div class="mb-4 w-full md:mb-0 md:w-auto text-center">
            <div id="continue-disabled" class="bg-gray-200 text-white px-4 py-4 w-full rounded cursor-not-allowed">Door naar @(Model.NextStep.Name)</div>
            <button id="continue" class="bg-@(Model.CheckoutPage.PrimaryColor) text-white px-4 py-4 w-full rounded hover:bg-gray-900" type="submit" style="display: none;">Door naar @(Model.NextStep.Name)</button>
        </div>
    </div>
}